
//login or confirmation button, will make this green or blue depending on how I want to style the site
const ConfirmButton = (props) => {
    return ( 
        <button>{props.label}</button>
     );
}
 
const DeleteButton = (props) => {
    return ( 
        <button>{props.label}</button>
     );
}

const AddExpenseButton = (props) => {
    return ( 
        <button>{props.label}</button>
     );
}
 



export {ConfirmButton, DeleteButton, AddExpenseButton}
 
